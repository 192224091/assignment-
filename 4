#include <stdio.h>

int largestElement(int arr[], int n) {
    int max = arr[0];
    for(int i = 1; i < n; i++) {
        if(arr[i] > max) {
            max = arr[i];
        }
    }
    return max;
}

int main() {
    int n, arr[100];
    printf("Enter the number of elements to be stored in the array: ");
    scanf("%d", &n);
    printf("Enter %d elements in the array: \n", n);
    for(int i = 0; i < n; i++) {
        printf("element - %d : ", i);
        scanf("%d", &arr[i]);
    }
    printf("The largest element in the array is: %d\n", largestElement(arr, n));
    return 0;
}
Output Enter the number of elements to be stored in the array: 5
Enter 5 elements in the array: 
element - 0 : 1
element - 1 : 2
element - 2 : 3
element - 3 : 4
element - 4 : 5
The largest element in the array is: 5
